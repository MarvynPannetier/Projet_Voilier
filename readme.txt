Pense-bete

git config --global user.name "votre_pseudo"

git config --global user.email moi@email.com

- Cloner le repo github :

git clone https://github.com/MarvynPannetier/Projet_Voilier.git repertoire

note: le clone est une opération one-shot, faire un git pull les fois suivantes


- Modifier ensuite les fichiers dans l'arborescence "repertoire" avec votre editeur prégéré



- Prise en compte des modification et livraison dans le repo github

git commit -a -m "la raison de la modification"

git push
